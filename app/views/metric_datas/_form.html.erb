<%= form_for(@metric_data) do |f| %>
  <% if @metric_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metric_data.errors.count, "error") %> prohibited this metric_data from being saved:</h2>

      <ul>
      <% @metric_data.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :metric_id, "Metric" %><br />
    <%#= select_tag "metrics", options_from_collection_for_select(@metrics, "id", "name") %>
    <%= f.select(:metric_id, options_from_collection_for_select(@metrics, "id", "name")) %>

  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
