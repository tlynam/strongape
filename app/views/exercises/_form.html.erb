<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workout_id %><br />
    <%= f.text_field :workout_id %>
  </div>
  <div class="field">
    <%= f.label :exercise_name %><br />
    <%= f.text_field :exercise_name %>
  </div>
  <div class="field">
    <%= f.label :exercise_number %><br />
    <%= f.text_field :exercise_number %>
  </div>
  <div class="field">
    <%= f.label :number_of_exercise_groups %><br />
    <%= f.text_field :number_of_exercise_groups %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
